overwrite: true
schema: "http://localhost:4000/graphql"
documents: "../graphql/**/*.graphql"
generates:
  generatedTypes/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
